from PIL import Image



# Define the input and output file paths

input_image_path = "/mnt/data/file-3g4LFwBM9RwLq2zDdAevF3"

output_favicon_path = "/mnt/data/favicon.ico"



# Open the image

image = Image.open(input_image_path)



# Convert the image to ICO format with multiple standard favicon sizes

favicon_sizes = [(16, 16), (32, 32), (48, 48), (64, 64), (128, 128), (256, 256)]

image.save(output_favicon_path, format="ICO", sizes=favicon_sizes)



# Provide the favicon file for download

output_favicon_path
